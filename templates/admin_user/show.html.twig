{% extends 'base.html.twig' %}

{% block title %}AdminUser{% endblock %}

{% block body %}
    <h1>Détails de l'utilisateur</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ admin_user.id }}</td>
        </tr>
        <tr>
            <th>Username</th>
            <td>{{ admin_user.username }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ admin_user.email }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_user_index') }}">Retour à la liste</a>

    <a href="{{ path('admin_user_edit', {'id': admin_user.id}) }}">Modifier</a>

    {{ include('admin_user/_delete_form.html.twig') }}
{% endblock %}
